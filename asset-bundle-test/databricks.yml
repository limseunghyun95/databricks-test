# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "asset-bundle-test"

sync:

  include:
  - resources/*.yml
  - resources/*/*.yml

  exclude:
  - src/*.lvdash.json

resources:
  dashboards:
    test_view:
      display_name: "test-view"
      warehouse_id: 7a8df1a659d394c3
      file_path: ../src/test_view.lvdash.json

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-0f40ae9d-f377.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-0f40ae9d-f377.cloud.databricks.com
      # We always use /Users/seunghyun.lim@bespinglobal.com for all resources to make sure we only have a single copy.
      root_path: /Users/seunghyun.lim@bespinglobal.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as seunghyun.lim@bespinglobal.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: seunghyun.lim@bespinglobal.com
